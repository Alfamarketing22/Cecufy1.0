# Clave para entrar al panel /admin
ADMIN_PASSWORD=changeme

# String de conexion a Postgres (Neon / Vercel Postgres).
# Si se deja vacio, la app local usa un almacen en db/local-data.json.
DATABASE_URL=

# Puerto del servidor de API en desarrollo local
API_PORT=3001
